import "./Hero.css";

export default function Hero() {
  return (
    <section className="hero">
      <h2 className="hero-title">AI Age &amp; Gender Prediction</h2>
      <p className="hero-sub">
        Upload a face image and let our AI model estimate the person's age and
        gender.
      </p>
    </section>
  );
}
